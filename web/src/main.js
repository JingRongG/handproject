// The Vue build version to load with the `import` command
// (runtime-only or standalone) has been set in webpack.base.conf with an alias.
import Vue from 'vue'
import router from './router'
import App from './App'
import jQuery from './components/scripts/jquery-3.2.1.min'
import SockJS from './components/scripts/sockjs'
import Stomp from './components/scripts/stomp.min'

Vue.config.productionTip = false

/* eslint-disable no-new */
new Vue({
  el: '#app',
  router,
  template: '<App/>',
  components: { App }
})
